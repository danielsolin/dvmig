<UserControl x:Class="dvmig.App.Views.MigrationDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Status Stats -->
        <UniformGrid Columns="3" Margin="0,0,0,10">
            <Border Margin="10" Padding="15" Background="#E8F5E9" CornerRadius="8">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="Success" Foreground="#2E7D32" FontWeight="Bold" />
                    <TextBlock Text="{Binding Progress.SuccessCount}" FontSize="36" FontWeight="Bold" Foreground="#2E7D32" HorizontalAlignment="Center" />
                </StackPanel>
            </Border>

            <Border Margin="10" Padding="15" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="Progress" FontWeight="Bold" HorizontalAlignment="Center" />
                    <ProgressBar Value="{Binding Progress.ProgressPercentage}" Height="20" Width="150" Margin="0,15,0,0" />
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,5,0,0">
                        <TextBlock Text="{Binding Progress.ProgressPercentage, StringFormat={}{0:F0}%}" />
                        <TextBlock Text=" (" />
                        <TextBlock Text="{Binding Progress.ProcessedRecords}" />
                        <TextBlock Text="/" />
                        <TextBlock Text="{Binding Progress.TotalRecords}" />
                        <TextBlock Text=")" />
                    </StackPanel>
                </StackPanel>
            </Border>

            <Border Margin="10" Padding="15" Background="#FFEBEE" CornerRadius="8">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="Failures" Foreground="#C62828" FontWeight="Bold" />
                    <TextBlock Text="{Binding Progress.FailureCount}" FontSize="36" FontWeight="Bold" Foreground="#C62828" HorizontalAlignment="Center" />
                </StackPanel>
            </Border>
        </UniformGrid>

        <!-- Logs -->
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <TextBlock Text="Migration Log" FontWeight="Bold" Margin="0,0,0,10" />
            <Border Grid.Row="1" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8">
                <ListBox x:Name="LogList" ItemsSource="{Binding Logs}" FontFamily="Consolas" FontSize="12" />
            </Border>
        </Grid>

        <!-- Actions -->
        <Grid Grid.Row="2" Margin="0,10,0,0">
            <Button Command="{Binding GoBackCommand}" 
                    Content="← BACK" Padding="20,10" 
                    Background="#7986CB" Foreground="White" 
                    BorderThickness="0" HorizontalAlignment="Left" />

            <Button Padding="20,10" Foreground="White" BorderThickness="0" HorizontalAlignment="Center">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#3F51B5" />
                        <Setter Property="Content" Value="START" />
                        <Setter Property="Command" Value="{Binding StartMigrationCommand}" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsMigrationRunning}" Value="True">
                                <Setter Property="Content" Value="STOP" />
                                <Setter Property="Command" Value="{Binding CancelMigrationCommand}" />
                                <Setter Property="Background" Value="#F44336" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Grid>
</UserControl>
