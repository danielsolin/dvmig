<UserControl x:Class="dvmig.App.Views.EntitySelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Search -->
        <Border Padding="10" Background="White" BorderBrush="#E0E0E0" 
                BorderThickness="1" CornerRadius="8" Margin="0,0,0,10">
            <DockPanel>
                <TextBlock Text="Search:" VerticalAlignment="Center" Margin="0,0,10,0" />
                <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
            </DockPanel>
        </Border>

        <!-- Entity Lists -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="20" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Left List: Available Entities -->
            <Border Grid.Column="0" Background="White" BorderBrush="#E0E0E0" 
                    BorderThickness="1" CornerRadius="8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock Text="Available Entities" FontWeight="Bold" Margin="10,10,10,5" Foreground="#757575" />
                    <ListBox Grid.Row="1" ItemsSource="{Binding Entities}" HorizontalContentAlignment="Stretch" BorderThickness="0">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="#F5F5F5" BorderThickness="0,0,0,1" Padding="10">
                                    <DockPanel LastChildFill="True">
                                        <Button DockPanel.Dock="Right" 
                                                Command="{Binding DataContext.ToggleSelectionCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                                CommandParameter="{Binding}"
                                                Content="ADD →" 
                                                Padding="10,5" FontSize="10" 
                                                Background="#4CAF50" Foreground="White" BorderThickness="0" />
                                        
                                        <StackPanel VerticalAlignment="Center">
                                            <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" />
                                            <TextBlock Text="{Binding LogicalName}" FontSize="12" Foreground="Gray" />
                                        </StackPanel>
                                    </DockPanel>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>

            <!-- Right List: Selected Entities -->
            <Border Grid.Column="2" Background="White" BorderBrush="#E0E0E0" 
                    BorderThickness="1" CornerRadius="8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock Text="Selected Entities" FontWeight="Bold" Margin="10,10,10,5" Foreground="#757575" />
                    <ListBox Grid.Row="1" ItemsSource="{Binding SelectedEntitiesView}" HorizontalContentAlignment="Stretch" BorderThickness="0">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="#F5F5F5" BorderThickness="0,0,0,1" Padding="10">
                                    <DockPanel LastChildFill="True">
                                        <Button DockPanel.Dock="Left" 
                                                Command="{Binding DataContext.ToggleSelectionCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                                CommandParameter="{Binding}"
                                                Content="← REMOVE" 
                                                Padding="10,5" FontSize="10" 
                                                Background="#F44336" Foreground="White" BorderThickness="0" 
                                                Margin="0,0,10,0"/>

                                        <StackPanel VerticalAlignment="Center">
                                            <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" />
                                            <TextBlock Text="{Binding LogicalName}" FontSize="12" Foreground="Gray" />
                                        </StackPanel>
                                    </DockPanel>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>

            <!-- Loading Overlay -->
            <Border Grid.ColumnSpan="3" Background="#80FFFFFF" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                <TextBlock Text="Loading Metadata..." HorizontalAlignment="Center" 
                           VerticalAlignment="Center" FontWeight="Bold" />
            </Border>
        </Grid>

        <!-- Action -->
        <Grid Grid.Row="2" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            
            <Button Command="{Binding GoBackCommand}" 
                    Content="← BACK" 
                    Padding="20,10" Background="#9E9E9E" Foreground="White" 
                    BorderThickness="0" Grid.Column="0" />

            <Button Command="{Binding StartMigrationCommand}" 
                    Content="NEXT →" 
                    Padding="20,10" Background="#3F51B5" Foreground="White" 
                    BorderThickness="0" Grid.Column="2" />
        </Grid>
    </Grid>
</UserControl>
