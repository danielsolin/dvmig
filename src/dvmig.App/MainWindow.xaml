<Window x:Class="dvmig.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:viewmodels="clr-namespace:dvmig.App.ViewModels"
        xmlns:views="clr-namespace:dvmig.App.Views"
        mc:Ignorable="d"
        Title="Dataverse Migrator" Height="650" Width="1000"
        WindowStartupLocation="CenterScreen"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{materialDesign:MaterialDesignFont}">

    <Window.Resources>
        <DataTemplate DataType="{x:Type viewmodels:ConnectionViewModel}">
            <views:ConnectionView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:EntitySelectionViewModel}">
            <views:EntitySelectionView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:MigrationDashboardViewModel}">
            <views:MigrationDashboardView />
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Top Bar / Stepper Placeholder -->
        <materialDesign:ColorZone Mode="PrimaryMid" Padding="16">
            <StackPanel Orientation="Horizontal">
                <materialDesign:PackIcon Kind="DatabaseSync" Width="24" Height="24" />
                <TextBlock Margin="16,0,0,0" VerticalAlignment="Center" 
                           Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                           Text="Dataverse Migrator" />
            </StackPanel>
        </materialDesign:ColorZone>

        <!-- Main Content -->
        <ContentControl Grid.Row="1" Content="{Binding CurrentViewModel}" 
                        Margin="16">
            <ContentControl.ContentTemplate>
                <DataTemplate>
                    <TextBlock Text="Welcome to dvmig. Initialization in progress..." 
                               HorizontalAlignment="Center" VerticalAlignment="Center"
                               Style="{StaticResource MaterialDesignHeadline4TextBlock}" />
                </DataTemplate>
            </ContentControl.ContentTemplate>
        </ContentControl>

        <!-- Bottom Status Bar -->
        <StatusBar Grid.Row="2" Background="{DynamicResource MaterialDesignSelection}">
            <StatusBarItem>
                <TextBlock Text="Ready" />
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
