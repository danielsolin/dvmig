<Window x:Class="dvmig.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:dvmig.App.ViewModels"
        xmlns:views="clr-namespace:dvmig.App.Views"
        mc:Ignorable="d"
        Title="Dataverse Migrator" Height="650" Width="1000"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <DataTemplate DataType="{x:Type viewmodels:ConnectionViewModel}">
            <views:ConnectionView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:EntitySelectionViewModel}">
            <views:EntitySelectionView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:MigrationDashboardViewModel}">
            <views:MigrationDashboardView />
        </DataTemplate>
    </Window.Resources>

    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="60" />
            <RowDefinition Height="*" />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Background="#3F51B5" Padding="20,0">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="DVMIG" Foreground="White" 
                           FontSize="24" FontWeight="Bold" />
                <TextBlock Text="Dataverse Migrator" Foreground="White" 
                           FontSize="18" Margin="15,0,0,0" VerticalAlignment="Bottom" 
                           Opacity="0.8" />
            </StackPanel>
        </Border>

        <!-- Content -->
        <ContentControl Grid.Row="1" Content="{Binding CurrentViewModel}" />

        <!-- Status Bar -->
        <Border Grid.Row="2" Background="#E0E0E0" BorderBrush="#BDBDBD" BorderThickness="0,1,0,0">
            <StatusBar Background="Transparent">
                <StatusBarItem>
                    <TextBlock Text="Ready" FontSize="12" Foreground="#616161" />
                </StatusBarItem>
            </StatusBar>
        </Border>
    </Grid>
</Window>
